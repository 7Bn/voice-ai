# TODOS

Generated by /plan-eng-review — 2026-03-31

## Blocking (must resolve before writing a line of voice code)

- [ ] B2/B5: Sign Anthropic HIPAA BAA — contact enterprise@anthropic.com. Required before any patient audio flows through Claude API.
- [ ] B3/B6: Confirm ElevenLabs HIPAA BAA availability. If unavailable, use Cartesia (BAA available, LiveKit Agents plugin). Decide before TTS integration starts.
- [ ] B1: PMS integration v0.1 scope is "fake it" (capture intent → SMS/email to practice). Confirm first paying client is OK with manual PMS confirmation by staff. Document explicitly in pilot terms.

## Architecture decisions (write to design doc before coding)

- [ ] Choose database: PostgreSQL + asyncpg + SQLAlchemy async. Document schema (practices, calls, audit_log) before first migration.
- [ ] Define PMS provider interface (PMSClient protocol) even though v0.1 skips PMS — so the abstraction is there when Dentrix/Eaglesoft is added.
- [ ] Define STT/TTS provider abstraction (Protocol classes) before wiring LiveKit Agents — enables Sarvam swap without rework.
- [ ] Document call disposition states: BOOKING_CAPTURED / ESCALATED / ESCALATED_UNANSWERED / HUNG_UP / FAQ_ONLY / DUPLICATE_PREVENTED. v0.1 uses BOOKING_CAPTURED instead of BOOKED.

## Implementation order (v0.1)

1. [ ] FastAPI skeleton + Twilio webhook endpoint (POST /twilio/voice)
2. [ ] Practice lookup by Twilio phone number (DB + seed first test practice)
3. [ ] LiveKit Agents integration — Deepgram STT + Claude LLM + ElevenLabs TTS pipeline
4. [ ] Conversation state machine (6 states: GREETING → IDENTIFY → INTENT → COLLECT → CONFIRM → COMPLETE)
5. [ ] Escalation engine — keyword triggers + FastAPI → Twilio REST API warm transfer with whisper
6. [ ] Call logging — PostgreSQL write + S3 audio upload (AES-256 SSE-KMS) at call end
7. [ ] HIPAA audit log middleware — cross-cutting FastAPI dependency
8. [ ] Recording disclosure + SMS opt-in in LLM system prompt (parameterized by practice state for CA two-party consent)
9. [ ] Stripe subscription billing — per-practice Stripe customer, two plans ($349 Starter / $599 Growth)
10. [ ] Weekly email digest cron — pull call log metrics, send via SES/SendGrid every Monday

## Testing (write tests alongside each feature, not after)

- [ ] Call routing: practice found → agent starts; practice not found → graceful hangup
- [ ] Conversation: booking captured, escalation trigger, escalation unanswered, hangup, 4-min timeout
- [ ] Booking deduplication: same patient/phone/day → confirm not duplicate
- [ ] HIPAA: recording disclosure in first utterance, audit log entry on transcript access
- [ ] Escalation: whisper text correct, Twilio conference initiated, LiveKit exits room after transfer
- [ ] Billing: Stripe subscription created, call rejected if subscription lapsed
- [ ] Edge cases: STT empty transcript → "didn't catch that" prompt; duplicate Twilio webhook → idempotent; practice timezone affects after-hours correctly

## Pre-production checklist (before first real patient call)

- [ ] Anthropic BAA signed
- [ ] ElevenLabs BAA signed (or Cartesia substituted)
- [ ] LiveKit BAA signed (confirmed available)
- [ ] Twilio BAA signed
- [ ] Deepgram BAA signed
- [ ] AWS BAA signed (covers S3, RDS, SES)
- [ ] Practice BAA signed with client
- [ ] Call recording disclosure wording reviewed by lawyer (especially CA two-party consent)
- [ ] S3 bucket: us-east-1, SSE-KMS, 6-year lifecycle policy, bucket policy blocks public access
- [ ] PostgreSQL: encrypted at rest (RDS with encryption enabled), TLS-only connections
- [ ] AWS Secrets Manager: all credentials stored (Twilio tokens, Stripe keys, Anthropic key, ElevenLabs key, LiveKit credentials)
- [ ] No .env files with real credentials committed to git

## Deferred (not v0.1)

- [ ] PMS write integration (Dentrix, Eaglesoft) — v0.2, after first paying clients confirm which PMS they're on
- [ ] Web dashboard — month 3+, after billing and onboarding are stable
- [ ] Self-serve onboarding — deferred until 20+ clients
- [ ] Sarvam multilingual support — pluggable via STT/TTS provider interface (interface built in v0.1, implementation deferred)
- [ ] Multi-location support
- [ ] Vet clinic integrations (Phase 2 after 10 dental clients)
